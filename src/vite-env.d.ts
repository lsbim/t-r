// TS에서 Vite 환경변수를 사용하기 위해 생성한 코드

interface ImportMetaEnv {
    readonly VITE_TIMELINE_PIXELS_PER_DAY: number
    readonly VITE_TIMELINE_BASE_DATE_HEIGHT: number
}

interface ImportMeta {
    readonly env: ImportMetaEnv
}