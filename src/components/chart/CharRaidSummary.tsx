import { CharacterSeasonData } from "../../types/commonTypes";

const CharRaidSummary = ({ data }: { data: Record<string, CharacterSeasonData[]> }) => {
    return (
        <div>

        </div>
    );
}

export default CharRaidSummary;